# SC01E04 - TypeScript & Architecture

## Menu du jour

- Lecture Correction (30min)
  - Modélisation YouTube
  - API spécifications

- Introduction à TypeScript (2h)
  - Intérêt & exécution

- Architecture TypeScript (2h)
  - Mise en place de l'API

## TypeScript

- Compilation : 
  - du code lisible par un humain ==> un code lisible par un ordinateur (un exécutable donc)
- Transpilation : 
  - du code lisible par un humain ==> un code lisible par un humain


## Exécution du TypeScript 

Avec Node :

Option 1 : 
- compiler le code TS vers JS à l'aide du transpilateur `TSC`
- plutôt cette approche là en production (on le fera cette aprem' avec l'API)

Option 2 : 
- Utiliser de la compilation à la volée avec `TSX`
- avantages : pratique et mature
- inconvénient : pas prévu pour la production (bootstrap long et transpilation à la volée)

## Types de base

Types primitifs
- `string`
- `number`
- `boolean`

Types nullable
- `undefined` : type undefined
- `null` : type null
- `void` : "cette fonction ne retourne rien"

Autres
- `any` : n'importe quel type --> on peut tout faire avec la varialbe, comme en JS
- `unknown` : "je ne maitrise pas ce qu'il y a dans la variable" --> vérification nécessaire des types à faire avant de s'en servir
- `never` : "il n'y a RIEN dans la variable"


## Fonction `.map()`

Entrée : tableau d'ELEMENTS
Sortie : tableau d'ELEMENTS (a qui on a fait subir une opération)

```js
const fruits = ["kiwi", "mango", "apple"];

const fruitsUpperCased = fruits.map(fruit => fruit.toUpperCase());

console.log(fruits); // ["kiwi", "mango", "apple"]
console.log(fruitsUpperCased); // [ 'KIWI', 'MANGO', 'APPLE' ]
```

## Commandes NPM

- `npm init` : créer le package.json
- `npm install xxxxx` : permet d'installer un package =
  - le déclarer dans le `package.json`
  - le télécharger dans les `node_modules`
- `npm install` : permet d'installer tous les packages déclarés dans le `package.json`
- `npm install --save-dev xxxxx` : déclarer comme dépendance de dev 
  - pour le local
  - pour produire le code qui sera ensuite exécuter en production
- `npx <command>` : exécuter une commande dont l'exécutable (binaire) se trouve dans les `node_modules`

Pourquoi pas installer TS au global (voire même avec `--global`) : 
- car `npm create vite@latest` gère sa propre config TS

Notre `package.json` global nous servira uniquement à : 
- déclarer les métadonnées du projet
- utiliser l'option `--prefix api` ou `--prefix client` pour gérer les différents services

## Déroulé de la mise en place de l'API

```
==== API ====
- mkdir api
- cd api
- npm init -y
- type module
- gitignore
- npm install --save-dev typescript tsx
- npx tsc --init


----------------
{
  // Ce fichier gère la manière dont le compilateur TSC va compiler le code TS vers du JS
  
  "compilerOptions": {
    "target": "esnext",               // Language cible pour la compilation TS -> dernière version de JS
    "module": "nodenext",             // Type de résolution cible pour les modules (ESM)
    "moduleResolution": "nodenext",   // Type de résolution source pour les modules (ESM)
    "rootDir": "./",                  // Où se trouve notre code TS
    "outDir": "./dist",               // Où sera généré notre code JS
    
    "forceConsistentCasingInFileNames": true, // Éviter les problèmes de casing dans les noms de fichier
    "rewriteRelativeImportExtensions": true,  // Autoriser les imports avec l'extension .ts
    
    "strict": true,                   // Règle de syntaxe TS à respecter
    "erasableSyntaxOnly": true,       // Utiliser uniquement la syntaxe supportées par Node.js (pas d'enum, décorateurs...)
    "verbatimModuleSyntax": true,     // Avoir les imports de type via `import type`
    
    "skipLibCheck": true              // Ne pas checker le code TS des fichier .d.ts
  }
}
----------------


- touch index.ts
- TESTER : npx tsx index.ts
- TESTER : npx tsx ---prefixatch index.ts
- TESTER : npx tsc

- gitignore pour le dossier dist

- npm install --save-dev @types/node
- npm install express 
- npm install --save-dev @types/express


----------------
import express from "express";

const app = express();

app.get("/health", (req, res) => {
  const up = true;
  const url = req.url;
  const ip = req.ip;
  const date = new Date().toISOString();
  res.json({ up, url, ip, date });
});

const port = 3000;
app.listen(port, () => {
  console.info(`🚀 Server started at http://localhost:${port}`);
});
----------------


- ./config.ts et typage
- .env (gitignoré)
- .env.example
- Flag --env-file=.env
- scripts NPM : 
  - dev
  - start
  - build


- routers/index.router.ts
- routers/users.router.ts
- controllers/users.controller.ts

----------------
import type { Request, Response } from "express";

export async function getAllUsers(req: Request, res: Response) {
  res.json([]);
}
---------------
```



## Déroulée Prisma

```
- npm install prisma --save-dev
- modifier la config schema.prisma

--------------
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client"
  output   = "./generated"
}

model User {
  id Int @id @default(autoincrement())

  firstname  String
  lastname   String
  email      String  @unique()
  password   String

  created_at DateTime @default(now())
  updated_at DateTime @default(now()) @updatedAt

  @@map("user") // Correspondance entre le model User dans le code et la table "user" dans la BDD
}
--------------

- Ajouter DATABASE_URL au .env
- Ajouter `prisma/generated` dans le gitignore


====== CREATION DE LA BASE DE DONNEES ====

- CREATE ROLE oquiz WITH LOGIN CREATEDB PASSWORD 'oquiz';
- CREATE DATABASE oquiz WITH OWNER oquiz;
- Tester : npx prisma migrate dev
- Tester : npx prisma studio

- Ajouter : src/models/index.ts (pour le client de connexion) à exporter
- puis faire le call dans getAllUsers() et vérfier l'API avec Chrome ou autre HTTP client

- Seeding : 
  - src/models/seed.ts --> créer quelques User de test, et profiter de l'auto complétion TS

- Ajouts des scripts NPM pour se faciliter la vie (voir paragrpahe suivant)
```
